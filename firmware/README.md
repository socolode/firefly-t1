# firmware · 固件目录

存放萤火虫T1 的官方固件文件。

## 目录结构

```
firmware/
├── stable/   # 稳定版固件（推荐日常使用）
└── beta/     # 测试版固件（含新功能，可能存在不稳）
```

## 刷机方法

1. 萤火虫T1 上电时**按住中键**进入刷机模式，电脑会出现名为 `FIREFLY-T1` 的 U 盘
2. 把对应版本的 `.uf2` 文件拖入 U 盘根目录
3. 设备自动重启，刷机完成

> 固件即将上传，敬请期待。
